// Counterexample: a file merely ENDING in "env"-ish text must not be treated
// as an environment file. Only real dotenv conventions (*.env, .env.*) count.
export const ENV_DOCS = "see docs/environment.md for runtime configuration";
