// Counterexample fixture: bare mentions of "privacy policy" / "consent" in
// code (checklist strings, TODO comments) must NOT count as a consent
// artifact — only links, routes, or dedicated files do.
export const LAUNCH_CHECKLIST = [
  "add a privacy policy page before launch",
  "add cookie consent banner",
  "review terms of service template",
];
